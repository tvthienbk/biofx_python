# Bayesian Workflow Portfolio — pinned runtime
# Tested with Python 3.11. Install: pip install -r requirements.txt
pymc==5.28.5
arviz==0.23.4
numpy>=2.0,<3
scipy>=1.13
pandas>=2.0
matplotlib>=3.8
nbformat>=5.10
# Optional but recommended for running notebooks / tests:
jupyter>=1.0
pytest>=8.0
