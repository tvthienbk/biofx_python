# Project 07 — environment (pin before tagging your release)
# Core (light, T4-friendly) — installed by 00_setup.ipynb
biopython==1.84
py3Dmol==2.4.0
numpy
pandas
matplotlib
seaborn
scikit-learn
tqdm
requests

# Heavy / on-demand (install via 00_setup helpers; pin the version you actually used):
# BindCraft / FreeBindCraft  — github.com/martinpacesa/BindCraft  (A100; FreeBindCraft = free-tier fallback, VERIFY)
# RFdiffusion (binder mode)  — github.com/RosettaCommons/RFdiffusion
# ColabDesign                — github.com/sokrypton/ColabDesign  (binder protocol + ProteinMPNN)
# colabfold                  — github.com/sokrypton/ColabFold    (AF2-Multimer; pae_interaction)
#
# Record the EXACT resolved versions in your report (version-stamp cell in 00_setup.ipynb).
# Real BindCraft/RFdiffusion campaigns realistically want an A100 (see MANUAL.md §2).
