# Project 15 — environment (pin before tagging your release)
# Core (light, T4-friendly) — installed by 00_setup.ipynb and used by the notebooks/scripts.
biopython==1.84
py3Dmol==2.4.0
numpy
pandas
matplotlib
seaborn
tqdm
requests

# Scoring backends (LIGHT — CPU/T4 fine; this is the cheap, high-value part of the project).
# Install on demand and pin the version+commit you actually used — these tools change.
#
# fair-esm            # ESM / ESM-1v mutation scoring — https://github.com/facebookresearch/esm
#                     #   pip install fair-esm ; ensemble esm1v_t33_650M_UR90S_{1..5}; pin the commit.
# ablang              # AbLang antibody LM — https://github.com/oxpig/AbLang
#                     #   AbLang2 SUPERSEDES — VERIFY the current public repo at course start; pin it.
# ProteinMPNN         # CDR redesign (framework FIXED via a design mask) — https://github.com/dauparas/ProteinMPNN
#                     #   install per the repo; pin the commit. CPU-fine.
# anarci              # antibody numbering (IMGT/Kabat/Chothia) to define CDR boundaries — pin it.
#
# Heavy / on-demand — the SLOW step; batch it. Colab T4-Pro is the realistic tier.
# colabfold           # AF2-Multimer pose check — prefer the official ColabFold notebook; pin the commit.
# torch / transformers # backends for ESM/AbLang/ColabFold as required by each tool's install instructions.
#
# Developability: the maturation_tools.developability_scan() heuristics need NOTHING extra (pure Python).
# For REAL developability, install/pin the actual tools and cite them — they are NOT in this requirements
# file because they are the student's reportable choice:
#   - TAP (Therapeutic Antibody Profiler) / SAbDab tooling (Raybould 2019)
#   - CamSol (Sormanni 2015)
#   - a structure-based deamidation/isomerization/oxidation hotspot predictor
#
# Record the EXACT resolved versions + tool commits in LOG.md (use the 00_setup version stamp).
