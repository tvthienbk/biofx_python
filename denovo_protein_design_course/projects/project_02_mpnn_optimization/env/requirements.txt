# Project 02 — environment (pin before tagging your release)
# Core (light, T4-friendly) — installed by 00_setup.ipynb
biopython==1.84
py3Dmol==2.4.0
numpy
pandas
matplotlib
seaborn
scikit-learn          # the lightweight "likely expresses" model + clustering of settings
tqdm
requests

# Heavy / on-demand (install via the helpers in 00_setup; pin the version you actually used):
# transformers>=4.40   # ESMFold (EsmForProteinFolding) — recapitulation triage
# accelerate
# colabfold            # AlphaFold2 — recapitulation confirmation; prefer the official notebook; pin the commit
#
# Sequence design — installed from source (clone + pin a commit; NOT on PyPI):
#   ProteinMPNN  https://github.com/dauparas/ProteinMPNN   (pin commit; verify it exists)
#   LigandMPNN   https://github.com/dauparas/LigandMPNN    (pin commit; verify it exists)
# Both are torch-based and run CPU-fast/seconds; no special GPU needed for MPNN itself.
#
# Record the EXACT resolved versions + commit hashes in your report. On Colab, capture pip versions
# with the version stamp cell in 00_setup.ipynb and paste into LOG.md. The recapitulation predictors
# (ESMFold/AF2) are the compute bottleneck — note their versions too.
